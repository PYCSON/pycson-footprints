# V431Q Next Scope Plan

Recommended next scope: V431R_BOUNDED_SCHEDULER_MOCK_DRYRUN_PACKAGE_OR_HOLD

Reason: candidate screening is accepted at local/mock level, but live readonly refresh and scheduler behavior are not ready. A bounded scheduler mock dryrun package is the most conservative next step because it stays package-only and does not fetch Steam/BUFF or calculate EV.

Deferred: EV dryrun preconditions remain premature until refresh readiness and scheduler boundaries are accepted.

