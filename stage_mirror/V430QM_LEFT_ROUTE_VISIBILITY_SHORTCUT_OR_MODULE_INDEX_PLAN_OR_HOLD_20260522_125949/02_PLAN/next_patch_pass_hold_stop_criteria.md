# V430QN PASS / HOLD / STOP Criteria

PASS only if the patch adds a narrow visible module index / shortcut strip, improves findability for left route/V418/report-proof-git access, preserves all functions, and Git sync succeeds.

HOLD if the patch scope expands beyond the left shortcut/index, if screenshot evidence is insufficient, if the shortcut adds clutter without improving visibility, or if Git export is blocked.

STOP if Mother UI is modified, route entries are removed/hidden, Steam/V430PT/right dock/risk/replay/bottom/theme controls break, DATA_BRIDGE or active payload is written, EV/fetch/buy/trade/order occurs, FAICTORY is touched, or force/reset/delete occurs.
